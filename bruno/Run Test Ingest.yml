info:
  name: Run Test Ingest
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/ingest/run_test"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
