info:
  name: GET TIPPs
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/tipp"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
