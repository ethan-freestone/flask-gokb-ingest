info:
  name: Stream GOKB Ingest
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/ingest/gokb_stream"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
